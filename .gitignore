# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Nested git của skill copy (giữ file, bỏ .git con)
.claude/skills/**/.git/

# Secrets — không commit API key
**/config.local.json
.env

# Outputs chứa DATA THẬT của khách — KHÔNG commit lên repo public
outputs/*.json
outputs/*.xlsx
outputs/*.csv
# (Muốn commit file demo ẩn danh thì đặt tên khác + thêm ngoại lệ, vd !outputs/sample-*)
